alembic==1.13.2
bcrypt==4.0.1
celery==5.4.0
# Fernet, for the encrypted Workday credential store (app/profile/credentials.py)
# and the people email store (app/people/security.py). Previously satisfied only
# as an extra of `python-jose[cryptography]`; pinned explicitly here because
# removing python-jose would otherwise have broken credential decryption in
# production with a bare ModuleNotFoundError at import time.
cryptography==50.0.1
email-validator==2.2.0
fastapi==0.141.1
httpx==0.27.0
openai==1.40.6
passlib[bcrypt]==1.7.4
psycopg[binary]==3.2.1
phonenumbers==8.13.42
pydantic==2.13.0
pydantic-settings==2.4.0
pypdf==6.16.1
python-docx==1.1.2
# JWT signing/verification for the access token and the scoped assisted-apply
# tokens. Replaced python-jose 3.3.0, which carried an algorithm-confusion
# advisory (PYSEC-2024-232) and two JWE decompression advisories
# (PYSEC-2024-233, PYSEC-2025-185), and which pulled in `ecdsa` — whose Minerva
# timing advisory (PYSEC-2026-1325) the upstream project has said it will not
# fix. Neither library's vulnerable paths were reachable here (HS256 only, no
# JWE, no asymmetric keys), but an unfixable transitive dependency is not
# something to carry into a public release.
PyJWT==2.14.0
python-multipart==0.0.32
reportlab==4.2.2
redis==5.0.8
SQLAlchemy==2.0.32
# Pinned explicitly (not just via FastAPI) because this is the package that
# carries the form-parsing advisories fixed for SUP-DEP-001. Re-check the
# floor whenever FastAPI moves: >=0.40.0 for GHSA-f96h-pmfr-66vw and
# >=1.3.1 for GHSA-82w8-qh3p-5jfq.
starlette==1.6.0
uvicorn[standard]==0.30.5
